import 'package:flutter/material.dart';

/// Symphonia Color Palette
/// A carefully curated, romantic yet elegant color scheme
/// designed to feel premium, intimate, and emotionally expressive.
class AppColors {
  AppColors._();

  // ═══════════════════════════════════════════════════════════════════════════
  // PRIMARY PALETTE - Deep Rose & Coral tones
  // ═══════════════════════════════════════════════════════════════════════════

  /// Primary rose - Main brand color
  static const Color primary = Color(0xFFE85A7A);

  /// Light rose variant
  static const Color primaryLight = Color(0xFFFF8FA3);

  /// Deep rose for emphasis
  static const Color primaryDark = Color(0xFFC43D5C);

  /// Soft rose for backgrounds
  static const Color primarySoft = Color(0xFFFFF0F3);

  // ═══════════════════════════════════════════════════════════════════════════
  // SECONDARY PALETTE - Warm Amber & Gold accents
  // ═══════════════════════════════════════════════════════════════════════════

  /// Secondary warm golden tone
  static const Color secondary = Color(0xFFFFB347);

  /// Light amber
  static const Color secondaryLight = Color(0xFFFFD699);

  /// Deep amber for accents
  static const Color secondaryDark = Color(0xFFE59400);

  // ═══════════════════════════════════════════════════════════════════════════
  // ACCENT PALETTE - Lavender & Purple for depth
  // ═══════════════════════════════════════════════════════════════════════════

  /// Accent lavender
  static const Color accent = Color(0xFFB47ED8);

  /// Light lavender
  static const Color accentLight = Color(0xFFE1BEF5);

  /// Deep purple
  static const Color accentDark = Color(0xFF8B5CB8);

  // ═══════════════════════════════════════════════════════════════════════════
  // NEUTRAL PALETTE - Warm grays
  // ═══════════════════════════════════════════════════════════════════════════

  /// Pure white
  static const Color white = Color(0xFFFFFFFF);

  /// Off-white with warm tint
  static const Color offWhite = Color(0xFFFFFBFA);

  /// Light cream background
  static const Color cream = Color(0xFFFFF8F6);

  /// Warm light gray
  static const Color grayLight = Color(0xFFF5F0EE);

  /// Medium warm gray
  static const Color gray = Color(0xFFBDB5B2);

  /// Dark warm gray
  static const Color grayDark = Color(0xFF6B6360);

  /// Charcoal for text
  static const Color charcoal = Color(0xFF3D3836);

  /// Near black with warmth
  static const Color black = Color(0xFF1A1817);

  // ═══════════════════════════════════════════════════════════════════════════
  // SEMANTIC COLORS
  // ═══════════════════════════════════════════════════════════════════════════

  /// Success green
  static const Color success = Color(0xFF4CAF50);

  /// Warning amber
  static const Color warning = Color(0xFFFF9800);

  /// Error red
  static const Color error = Color(0xFFE53935);

  /// Info blue
  static const Color info = Color(0xFF2196F3);

  /// Hug amber
  static const Color hugAmber = Color(0xFFFFB347);

  /// Hug amber dark
  static const Color hugAmberDark = Color(0xFFFF8C00);

  /// Hug amber glow
  static const Color hugAmberGlow = Color(0xFFFFD700);

  // ═══════════════════════════════════════════════════════════════════════════
  // SPECIAL COLORS - For romantic effects
  // ═══════════════════════════════════════════════════════════════════════════

  /// Heart red - For the big heart button
  static const Color heartRed = Color(0xFFFF4D6D);

  /// Heart glow
  static const Color heartGlow = Color(0xFFFF8FA3);

  /// Blush pink for subtle highlights
  static const Color blush = Color(0xFFFFE5EC);

  /// Romantic deep red
  static const Color romanticRed = Color(0xFFD32F2F);

  // ═══════════════════════════════════════════════════════════════════════════
  // DARK MODE PALETTE
  // ═══════════════════════════════════════════════════════════════════════════

  /// Dark background
  static const Color darkBackground = Color(0xFF1A1517);

  /// Dark surface
  static const Color darkSurface = Color(0xFF2D2529);

  /// Dark card
  static const Color darkCard = Color(0xFF3D3539);

  /// Dark elevated surface
  static const Color darkElevated = Color(0xFF4D4549);
}
